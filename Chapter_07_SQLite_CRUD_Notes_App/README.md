# Chapter 07 - SQLite CRUD Notes App

Practice project for local data storage with SQLite.

The app can add, read, update, and delete notes using the `sqflite` package.

## Run

```bash
flutter pub get
flutter run
```
