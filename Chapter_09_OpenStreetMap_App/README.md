# Chapter 09 - OpenStreetMap App

Practice project for showing a map in Flutter.

The app uses `flutter_map`, `latlong2`, OpenStreetMap tiles, markers, and zoom buttons.

## Run

```bash
flutter pub get
flutter run
```
